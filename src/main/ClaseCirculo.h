// ClaseCirculo.h
#ifndef CLASECIRCULO_H
#define CLASECIRCULO_H

class ClaseCirculo {
private:
    double radio;
public:
    ClaseCirculo(double r);
    double calcularArea();
    double calcularPerimetro();
};

#endif // CLASECIRCULO_H

